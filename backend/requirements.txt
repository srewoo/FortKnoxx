# FortKnoxx backend — full install (BACKWARD-COMPAT).
#
# WHY this is now a thin file: dependencies were split into
# requirements-base.txt (always required) and requirements-ml.txt
# (optional GNN/CodeBERT stack). Existing tooling that runs
# `pip install -r requirements.txt` continues to get the full stack.
#
# For lighter installs (no GNN/CodeBERT):
#     pip install -r requirements-base.txt
#
# For ML-only environments (e.g., the future ai-engine-service):
#     pip install -r requirements-base.txt -r requirements-ml.txt

-r requirements-base.txt
-r requirements-ml.txt
